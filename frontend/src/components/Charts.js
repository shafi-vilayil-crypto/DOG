import { Area, AreaChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export function PerformanceChart({ data = [] }) { return <div className="chart-wrap" data-testid="performance-chart">{data.length ? <ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 12, right: 8, left: -22, bottom: 0 }}><defs><linearGradient id="mintFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#12b886" stopOpacity={0.2} /><stop offset="100%" stopColor="#12b886" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="#edf2f0" vertical={false} /><XAxis dataKey="time" tick={{ fontSize: 11, fill: "#8b9a96" }} tickLine={false} axisLine={false} /><YAxis tick={{ fontSize: 11, fill: "#8b9a96" }} tickLine={false} axisLine={false} /><Tooltip contentStyle={{ border: "0", borderRadius: 12, fontSize: 12 }} /><Area type="monotone" dataKey="avg" stroke="#12a879" fill="url(#mintFill)" strokeWidth={2.5} /><Line type="monotone" dataKey="p95" stroke="#9ab6ad" strokeWidth={1.5} dot={false} /><Line type="monotone" dataKey="p99" stroke="#d5e1de" strokeWidth={1.5} dot={false} /></AreaChart></ResponsiveContainer> : <div className="muted" style={{ padding: 24 }}>No latency data recorded yet.</div>}</div>; }
export function SpendChart({ data = [] }) { return <div className="chart-wrap" data-testid="spend-chart">{data.length ? <ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid stroke="#edf2f0" vertical={false} /><XAxis dataKey="day" tick={{ fontSize: 11, fill: "#8b9a96" }} tickLine={false} axisLine={false} /><YAxis tick={{ fontSize: 11, fill: "#8b9a96" }} tickLine={false} axisLine={false} /><Tooltip contentStyle={{ border: "0", borderRadius: 12, fontSize: 12 }} /><Line type="monotone" dataKey="spend" stroke="#3478f6" strokeWidth={2.5} dot={{ r: 3, fill: "#fff", strokeWidth: 2 }} /></LineChart></ResponsiveContainer> : <div className="muted" style={{ padding: 24 }}>No spend data recorded yet.</div>}</div>; }
